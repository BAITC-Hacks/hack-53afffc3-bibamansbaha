# EKT — приёмка развёртывания (в работе)

Начало: 2026-09-23, исходный проверенный продукт `9762068e6146d419938be37b52af154144e55260`, отчёт `d2033d3f0ff6e6a0ec2675d6a955fa1a08cffc9c`.

Порядок: установить фактический существующий Brev target и SSH-доступ → проверить стоимость/автоостановку и persistent storage → развернуть проверенный кандидат с действующей моделью, fixture-каталогом и ограниченным демо-доступом → бесплатные серверные проверки → единый ограниченный платный smoke → HTTPS/browser, две сессии, restart/SQLite → полный отчёт.

Второй сервер не создаётся. Репозиторий не перезапускается с нуля. Исторические FINAL_AUDIT и REPAIR_REPORT сохраняются. EKT-002/017/018/019 не считаются закрытыми; серверная проверка EKT-020 будет записана отдельно.

На начало этапа: в рабочем репозитории нет сохранённых параметров Brev/deployment; CLI и существующий ресурс проверяются. Внешний URL и SSH пока не подтверждены. Платных вызовов этого smoke: 0 из максимум 6; потолок $0.25 в пределах общего бюджета, не возобновляемый. Ранее выполненные два вызова ($0.0003336) не считаются доступными повторно. Фактическая runtime-модель будет сверена с конфигурацией до вызовов.

Секреты, пользовательские БД, приватные документы и сырые журналы не публикуются. Предоставленный управляющий credential не попадёт в runtime приложения.

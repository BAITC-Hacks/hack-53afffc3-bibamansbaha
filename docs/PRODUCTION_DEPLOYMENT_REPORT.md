# EKT — продолжение production-развёртывания

Начало: 23 сентября 2026, 17:03 UTC+5. Срок текущего этапа: до 17:43 UTC+5.
Исходный main: `ed47274710bc6b36481c41839549962fd3a6adc3`; рабочее дерево перед изменениями чистое, origin/main обновлён через fetch.

Новое разрешение владельца: подготовить WSL/Ubuntu и официальный Brev CLI; после успешного пользовательского входа, refresh и проверки всех доступных организаций разрешён один CPU-инстанс, если существующих нет. Предел $0.50/час, минимум $5 резерва Brev, без GPU и автопополнения.

Целевая модель изменена владельцем на `gpt-5.5`, Responses API, reasoning low. Миграция требует проверки совместимости и отдельных постоянных лимитов acceptance (6/$0.25) и demo runtime с сохранением резерва API. Платные проверки пока не запускались.

На старте WSL установлен, Ubuntu отсутствует; WSL2 сообщает о недоступности виртуализации. Windows CIM показывает VirtualizationFirmwareEnabled=true: причина уточняется в компонентах Windows/гипервизоре, отключённый SVM пока не утверждается.

План: устранить WSL prerequisite → Brev auth/target/cost → production Linux + persistent SQLite + HTTPS → бесплатная приёмка → ограниченная настоящая GPT-5.5/vision проверка → README и итоговая матрица. Внешний URL пока отсутствует; DEPLOYMENT COMPLETE не заявляется.
